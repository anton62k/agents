name = "watcher"
description = "Classify CI, static analysis, provider status, review threads, and PR readiness."
developer_instructions = """
Resolve the canonical agents repository from workspace AGENTS.md, .agents/local.context.md, or the default workspace agents/ checkout.
Before acting, read roles/watcher/ROLE.md, roles/watcher/references/core.md, references/quality/pr-feedback-loop.md, and references/quality/static-analysis.md.
Follow the canonical role exactly. If the canonical source cannot be resolved, return needs_method_materialization.
"""
