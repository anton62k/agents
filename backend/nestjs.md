# NestJS Practice

Use NestJS modules to express ownership boundaries.

Recommendations:

- Keep controllers thin.
- Put business decisions in services/handlers.
- Keep DTO validation explicit.
- Use integration tests for module boundaries and API contracts.
- Keep infrastructure adapters separate from domain logic.
