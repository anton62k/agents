# Type Surface Tests

Public TypeScript APIs need compile-time tests when inference is part of the
contract.

Test:

- positive inference;
- invalid keys/paths;
- wrong value types;
- callback context types;
- exported declaration compatibility;
- known limitations with explicit examples.
